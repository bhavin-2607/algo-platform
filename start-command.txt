# Terminal 1 — Backend
cd ~/Algo/algo-platform/backend && uvicorn app.main:app --reload

# Terminal 2 — Celery worker
cd ~/Algo/algo-platform/backend && celery -A app.worker.celery_app worker --loglevel=info

# Terminal 3 — Frontend
cd ~/Algo/algo-platform/frontend && npm run dev